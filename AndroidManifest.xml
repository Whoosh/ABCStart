<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.First_prj"
          android:versionCode="1"
          android:versionName="1.0">
    <uses-sdk android:minSdkVersion="11"/>

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application android:icon="@drawable/logo">
        <activity android:name=".LoginForm.FirstActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".FirstActivitySettings.LoginFormSettingsActivity">
            <intent-filter>
                <action android:name="android.intent.action.GET_CONTENT"/>
                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>

        <activity android:name=".MenuAndSwitchers.MenuActivity">
        </activity>

        <activity android:name=".Journal.MainJournalActivityStarter">
        </activity>

        <activity android:name=".Journal.LookingJournalActivity">
        </activity>

        <activity android:name=".Journal.EditJournalActivity">
        </activity>

        <activity android:name=".Journal.StudentCheckerActivity">
        </activity>

    </application>
</manifest>
